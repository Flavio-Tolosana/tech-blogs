name: Deploy GitHub Pages

on:
  push:
    branches: [main, master]
    paths:
      - "dist/index.html"
      - "dist/sources.html"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Preparar sitio
        run: |
          mkdir -p _site
          cp dist/index.html _site/index.html
          cp dist/sources.html _site/sources.html

      - name: Subir artefacto
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./_site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Desplegar en GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5